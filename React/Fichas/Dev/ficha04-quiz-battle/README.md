# Quiz Battle

Implementação da `FICHA_04_QUIZ_BATTLE.md`.

Para testar localmente:

```bash
npm install
npm run dev
```

A app usa perguntas da Open Trivia DB e mantém perguntas locais como base interna/fallback.
